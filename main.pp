uses
  Colors,
  Math;
const
  WIDTH = 200;
  HEIGHT = 70;
var
  x, y: Byte;
  c: T4BitColor;
begin
  // for y := 1 to HEIGHT do begin
  //   for x := 1 to WIDTH do
  //     Write(TRGBColor.Create(
  //       Byte(Floor(255 / WIDTH * x)),
  //       0,
  //       Byte(Floor(255 / HEIGHT * y)),
  //       true
  //     ).S, ' ');
  //   WriteLn
  // end;
end.
